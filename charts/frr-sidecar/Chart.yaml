apiVersion: v2
name: frr-sidecar
description: |
  Shared FRR sidecar container template for k3s workloads.
  Owns OSPF config rendering and the PBR/transit env contract
  (single source of PBR_TRANSIT_TAG=201, mirroring
  roles/backbone_network/files/ospf_injector/frr_injector/config.py:TRANSIT_TAG).
type: library
version: 0.1.0
appVersion: "0.1.0"
